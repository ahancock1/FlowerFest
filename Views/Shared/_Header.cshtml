
<div class="header-inner">

    <!-- Logo -->
    <div class="logo">
        <a asp-controller="Home" asp-action="Index">
            <img src="~/img/logo-large.png"/>
        </a>
    </div>
    <!-- End Logo -->

    <!-- Mobile Navbar Icon -->
    <div class="nav-mobile nav-bar-icon">
        <span></span>
    </div>
    <!-- End Mobile Navbar Icon -->

    <!-- Navbar Navigation -->
    <div class="nav-menu singlepage-nav">
        <ul class="nav-menu-inner">
            <li>
                <a class="current" href="@Url.Action("Index", "Home", new {area = ""})#intro">Home</a>
            </li>
            
            @foreach (var section in Model.HeaderSections)
            {
                <li>
                    @{
                        var tag = section.Tag;
                        var url = $"{Url.Action("Index", "Home", new { area = "" })}#{tag}";
                    }

                    <a href="@url">@tag</a>
                </li>
            }
            
            <li>
                <a href="@Url.Action("Index", "Home", new {area = ""})#news">News</a>
            </li>

            <li>
                <a asp-controller="Blog" asp-action="Index" asp-area="Blog">Blog</a>
            </li>
            <li>
                <a href="@Url.Action("Index", "Home", new {area = ""})#contact">Contact</a>
            </li>

            @*@if (User.Identity.IsAuthenticated)
            {
                <li>
                    <a asp-controller="Account" asp-action="LogOutAsync">Logout</a>
                </li>
            }*@
        </ul>
    </div>
    <!-- End Navbar Navigation -->

</div>